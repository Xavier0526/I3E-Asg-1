using UnityEngine;

public class KeyCard : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    public string cardID = "BlueCard";
    public string displayName = "Blue Access Card";
}
